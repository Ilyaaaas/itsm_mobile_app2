import { StyleSheet } from 'react-native';

export const inputStyle = StyleSheet.create({
  inputItem: {
    borderRadius: 8,
    overflow: 'hidden',
    backgroundColor: 'white',
    marginTop: 8,
  },
});
